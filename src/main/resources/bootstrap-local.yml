server:
  port: 8887

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          #uri: ssh://cosmefulanito@localhost:2022/git_codes/cosmefulanito/cloud-config-tmp.git
          uri: https://github.com/redbeestudios/zelig-cloudconfig.git
          username: redbee-ofertador
          password: jenkins112358
          strictHostKeyChecking: false
management:
  endpoints:
    web:
      base-path: /
  endpoint:
    health:
      show-details: ALWAYS


encrypt:
  key: alagrandelepusecuca
