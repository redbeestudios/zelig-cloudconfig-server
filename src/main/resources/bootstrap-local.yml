server:
  port: 8887

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: ssh://cosmefulanito@localhost:2022/git_codes/cosmefulanito/cloud-config-tmp.git
          username: cosmefulanito
          password: 1234
          strictHostKeyChecking: false
management:
  endpoints:
    web:
      base-path: /
  endpoint:
    health:
      show-details: true


encrypt:
  key: alagrandelepusecuca
