server:
  port: 8888

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/development/repos/ms-cloud-config
management:
  endpoints:
    web:
      base-path: /
  endpoint:
    health:
      show-details: true