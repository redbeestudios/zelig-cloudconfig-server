server:
  port: 8888

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/redbeestudios/rfsc-ofertador-cloudconfig
          username: redbee-ofertador
          password: jenkins112358
management:
  endpoints:
    web:
      base-path: /
  endpoint:
    health:
      show-details: true
