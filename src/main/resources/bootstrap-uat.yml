server:
  port: 8887

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://repo.tools.rfsc.cl/scm/ofe/ofertador-cloud-config.git
          username: jenkins
          password: jenkins123456
management:
  endpoints:
    web:
      base-path: /
  endpoint:
    health:
      show-details: true
encrypt:
  key: alagrandelepusecuca