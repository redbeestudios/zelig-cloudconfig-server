server:
  port: 8888

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      env: desa
      server:
        git:
          uri: file://${user.home}/development/repositories/ms-cloud-config
encrypt:
  key: encryptme
security:
  user:
    password: sarasa
